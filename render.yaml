databases:
  - name: iniadfes_signage_db
    region: singapore
    plan: free
    databaseName: iniadfes_signage_db
    user: django_user

services:
  - type: web
    name: iniadfes_signage_site
    env: python
    region: singapore
    buildCommand: './build.sh'
    startCommand: 'gunicorn config.wsgi:application'
    plan: free
    branch: main
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: iniadfes_signage_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
    autoDeploy: true